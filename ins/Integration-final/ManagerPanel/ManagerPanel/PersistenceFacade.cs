using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Collections.ObjectModel;

namespace ManagerPanel
{
    class PersistenceFacade
    {
        /*protected ObservableCollection<Employee> _employees=new ObservableCollection<Employee>();
        protected List<Project> _projects = new List<Project>();
        public List<Project> GetProjecsOfManager()
        {
            _projects.Add(new Project("Project1"));
            _projects.Add(new Project("Project2"));
            _projects.Add(new Project("Project3"));
            _projects.Add(new Project("Project4"));

            return _projects;

        }

        public ObservableCollection<Employee> getEmployeesOfProject(Project p)
        {
            if (p != null)
                return p.Employees;
            else return new ObservableCollection<Employee>();
        }
        */
    }
}
